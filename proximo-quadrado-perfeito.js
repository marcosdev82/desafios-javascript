// Você sabe identificar alguns quadrados perfeitos, mas... e o próximo?

// Crie a função findNextSquare que recebe um número inteiro n e retorna o próximo quadrado perfeito após ele.

// Um quadrado perfeito é um número cuja raiz quadrada também é um número inteiro.

// Se n for um quadrado perfeito, retorne o próximo quadrado perfeito.

// Se n não for um quadrado perfeito, retorne -1 (ou null, None, dependendo da linguagem).

// Exemplos (entrada → saída):

function findNextSquare(sq) {
    // Return the next square if sq is a perfect square, -1 otherwise
    return -1;
}